/**
 * Dashboard index page. Phase 1 ships a placeholder while the collection grid
 * and items list are designed in Phases 2 and 3.
 * @returns The placeholder content for the main column.
 */
export default function DashboardPage() {
  return <h2 className="text-lg font-semibold">Main</h2>;
}
